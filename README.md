# 🐾 Wildlife Detection System 2026

A simple Streamlit app that detects animals using YOLOv8 and plays an alarm sound when detection occurs.

## How to run locally
```bash
pip install -r requirements.txt
streamlit run app.py
